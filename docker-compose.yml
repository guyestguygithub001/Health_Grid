version: '3.8'

services:
  plateau-ehr:
    build: .
    container_name: plateau-ehr-app
    ports:
      - "8082:8082"
    environment:
      - NODE_ENV=production
      - PORT=8082
    restart: unless-stopped
