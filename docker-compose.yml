version: '3.8'

services:
  web:
    build: .
    ports:
      - "8082:8082"
    environment:
      - NODE_ENV=development
    env_file:
      - .env
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
